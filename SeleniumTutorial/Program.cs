using OpenQA.Selenium;
using OpenQA.Selenium.Chrome;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using NUnit.Framework;

namespace SeleniumTutorial
{
    class Program
    {
        static void Main(string[] args)
        {
            /*Program program = new Program();
            program.StartBrowser();
            program.Test();
            program.CloseBrowser(); 

            GlobalNavigation gn = new GlobalNavigation();
            gn.StartBrowser();
            gn.Test();
            gn.CloseBrowser(); 

            LanguageSelector ls = new LanguageSelector();
            ls.StartBrowser();
            ls.CheckLanguageChange();
            ls.CloseBrowser(); */
        }
    }
}
